# include<bits/stdc++.h> 
# include <iostream>
# include <vector>
# include <stack>
using namespace std;

int main()
{
	int T, N, i, x, j, df, sumdf, mindf, maxdf;
	int X[100];
	stack<int> infected;
	cin >> T;
	while(T--)
	{
		cin >> N;
		for(i=0; i<N; i++)
		{
			cin >> X[i];
		}
		
		for(i=0; i<N; i++)
		{
			infected.push(X[i]);
			while(!infected.empty())
			{
				
			}
		}
		
	}
}
